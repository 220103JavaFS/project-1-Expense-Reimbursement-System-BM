package com.revature.models.users;

public interface Manager {
    //*note This interface really serves no purpose but is to show how it would add some functionality to other classes
    //if model type classes actually implemented methods of their own

    //METHODS
    /*
    public void hireEmployee(Employee emp);
    public void fireEmployee(Employee emp);
     */
}
